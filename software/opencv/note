Func: train(data, labels)

 - check data, labels arent erroneous
 - prepend data with column of 1s (bias/intercept)
 - init weights to size (features + 1, 1)
 - new_theta = batch_gradient_descent(
       data_t, labels, init_weights
   )


Func: batch_gradient_descent(
    data_t, labels, init_weights
)

 - check alpha, iters > 0
 - theta_p = init_theta.clone()
 - llambda = 1;
 - m = samples
